(function (module) {
    'use strict';
    module.config(RouteSetup);

    function RouteSetup($stateProvider, $urlRouterProvider) {
        // TODO: DEFINE ROUTES FOR THE APPLICATION
    }
}(angular.module('')));
