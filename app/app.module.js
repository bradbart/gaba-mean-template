(function () {
    'use strict';
    // TODO: provide name for the app
    angular.module('', ['ui.router']).config(AppSetup);

    function AppSetup() {
        // TODO: add constants for module
        console.log('No configuration set!');
    }
}());
